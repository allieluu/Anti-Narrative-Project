<div class="new post">
  <div class="ui middle aligned center aligned grid">
    <div class="three column">
      <h2 class="ui header teal">
        <div class="content">New Post</div>
      </h2>
      <%= form_for @post, url: posts_url, :html => {:class => 'ui large form'} do |f| %>
          <div class="ui stacked segment">
            <div class="required field">
              <%= f.text_field :title, placeholder: 'Title' %>
            </div>
            <!--<div class="hidden field">-->
            <%= f.hidden_field :user_id, value: current_user.id %>
            <!--</div>-->
            <div class="required field">
              <%= f.text_area(:content, placeholder: 'Share your story! (Minimum 10 characters)') %>
            </div>
            <!--<div class="ui fluid multiple search selection dropdown">-->
            <!--<%# f.hidden_field :all_tags %>-->
            <!--<i class="dropdown icon"></i>-->
            <!--<div class="menu">-->
            <!--<%# Tag.sorted.each do |tag| %>-->
            <!--<div class="item" data-value=<%# tag.name.to_s %>>-->
            <!--<%# tag.name.to_s %>-->
            <!--</div>-->
            <!--<%# end %>-->

            <%# f.collection_select :all_tags,
                                    Tag.sorted,
                                    :name,
                                    :name,
                                    {},
                                    {class: 'ui multiple search selection dropdown',
                                     id: 'multi-select',
                                     multiple: '',
                                    } %>
            <%= f.submit('Post', :class => 'ui fluid large teal submit button') %>
          </div>
      <% end %>
    </div>
  </div>
</div>

<footer>
  <% javascript_include_tag :posts %>
</footer>
